() // after a variable means we are calling a function with that name
=> // fat arrow
`` // template literal
${} // argument placeholder, what we place in between would be rendered dynamically at the runtime
... // spread operator


// Unintended side-effects // does bunch of stuff instead of just saving the user hence those our side-effects
function saveUser(user){ 
  // Validation for our user
  // Printing out all our validation error
  // Saving the user
}
